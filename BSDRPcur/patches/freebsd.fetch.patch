Index: lib/libfetch/fetch.c
===================================================================
--- lib/libfetch/fetch.c	(revision 314550)
+++ lib/libfetch/fetch.c	(working copy)
@@ -350,6 +350,7 @@ fetchParseURL(const char *URL)
 		fetch_syserr();
 		return (NULL);
 	}
+	u->netrcfd = -2;

 	/* scheme name */
 	if ((p = strstr(URL, ":/"))) {